class Solution {
public:
/*
    first thing that comes in my mind is the brute force approach given at each index 
    another one would be recursize which seems too slow at first but can be improved to O(N^2) using memoization

*/
    int lengthOfLIS(vector<int>& nums) {
        
    }
};