class Solution {
public:
    /*bool isPalindrome(string& binary){
        int left = 0, right = binary.size()-1;
        while(left <= right){
            if(binary[left] != binary[right]){
                return false;
            }
            left++;
            right--;
        }
        return true;
    }*/
    bool isStrictlyPalindromic(int n) {
        /*string binary = "";
        while(n){
            if((n & 1) == 1) binary.push_back('1');
            else binary.push_back('0');

            n >>= 1;
        }

        return isPalindrome*/
        return false;
    }
};