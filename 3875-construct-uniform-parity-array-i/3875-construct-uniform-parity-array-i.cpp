/*
    odd - odd = even
    even - odd = odd
    even - even = even

    approach count the odd and even elements

    if odd % 2 == 0 || even % 2 == 0 || odd: 
*/
class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        return true;
    }
};